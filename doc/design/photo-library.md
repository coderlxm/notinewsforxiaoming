# 照片墙功能设计文档交付计划

  ## 文档产出

  - 新建 doc/design/photo-wall-feature-design.md，记录产品定位、交互设计、数据链路、接口约定、部署调整
    与验收场景。

  - 文档以当前源码、rndc02 实际部署状态及 2026-08-22 的官方资料为依据，明确区分已确认事实与待实施内容。
  - 只交付设计文档，不修改功能代码、Drive 内容或部署环境。

  ## 设计定案

  - 在公开侧边栏“兴趣”下方新增“照片墙”，桌面端位于“关于我”上方；移动端底部导航调整为五栏。
  - 增加 /photos 作品集首页和 /photos/:albumId 相册详情页，继续使用现有公开页面外壳和局部滚动容器。
  - 首页采用“动态精选＋相册封面”：近期作品形成连续自动滚动首屏，相册按最新拍摄时间排列；相册封面取该相
    册最新照片。

  - 相册详情使用保留原始比例的专业画廊布局；照片随页面滚动产生克制的渐显、错层和轻微视差。
  - 选用 motion-v 同时实现自动循环与滚动关联动画，直接适配 Vue、自定义滚动容器和 reduced-motion；不在业
    务代码中实现 RAF。

  - 单图使用 PhotoSwipe 原生包提供沉浸查看、缩放、键盘和触摸切换，不采用陈旧的 Vue 包装层。
  - 网格不覆盖文字；沉浸查看器底部展示文件名标题、拍摄日期、相机、镜头、焦段、光圈、快门和 ISO，只展示
    JPEG 中实际存在的 EXIF。

  ## Drive 与服务端设计

  - Drive 根目录约定为 NotiNewsPhotos，第一层子文件夹对应相册，只接收后期完成的 sRGB JPEG；文件名去扩展
    名后作为照片标题。

  - rndc02 当前已有 notinews-drive: 和自有 OAuth 客户端，但授权范围为 drive.file，无法发现 Drive
    网页上传的文件；文档将要求一次性改为 drive.readonly。该限制与调整依据来自 Google Drive 权限说明
    (https://developers.google.com/workspace/drive/api/guides/api-specific-auth)及 rclone Drive 文档
    (https://rclone.org/drive/)。

  - 新增宿主机照片同步 service/timer，每约 5 分钟将专用 Drive 目录单向同步到 /opt/journal/photo-
    source；该目录只读挂载进 Journal 容器。Drive 是内容源，重命名和删除在下一同步周期反映到公开照片墙。

  - 同步成功后调用受现有内部凭据保护的重建入口；服务端顺序读取 JPEG、提取 EXIF，并通过 Sharp 生成模糊预
    览、卡片尺寸和沉浸查看尺寸，衍生文件保存在现有 /data 持久卷，不公开相机原始大图。

  - 建立照片墙索引，公开接口固定为作品集摘要、单相册详情和同源图片衍生资源；照片与相册 ID 由相对路径稳
    定派生，路径不直接作为文件系统访问参数。

  - 非 JPEG、目录层级不符合约定或处理失败时同步任务直接报错，不发布不完整的新索引，也不增加重试、降级或
    替代来源。

  - 文档记录部署机现状：Journal 容器目前仅挂载 /opt/journal/data，照片源挂载、同步服务和部署资产打包均
    属于后续实现范围。

  ## 公共接口与数据形态

  - 作品集摘要返回精选照片、相册 ID、名称、封面、照片数量和相册拍摄时间范围。
  - 相册详情返回按拍摄时间排列的照片，以及宽高、可空 EXIF 字段和各展示尺寸的同源 URL。
  - 图片资源根据照片 ID 和固定尺寸枚举读取，禁止接收任意本地路径。
  - 前端对已加载的首页和相册状态进行复用；路由切换只更新内容区，不让公共框架、侧边栏或已加载作品整页闪
    现。

  ## 验收场景

  - 桌面和移动端入口顺序、选中态、返回行为及五栏移动导航符合现有交互。
  - 首页自动滚动无跳缝；进入相册后滚动动画跟随现有 .app-scroll；系统减少动态效果时呈现静态布局。
  - Drive 网页新增、替换、重命名和删除相册或照片后，在一个同步周期内得到一致结果。
  - 横幅、相册封面、详情网格和沉浸查看器均保持原始比例、渐进加载和清晰度，不直接请求 Google Drive 缩略
    图或原图链接。

  - 单图查看支持桌面键盘、移动触摸与缩放，并只展示真实存在的 EXIF 数据。
  - 同步或图片处理错误在服务日志中明确暴露，公开索引不会被半成品覆盖。

  ## 调研依据与默认选择

  - 动画选择依据：Motion for Vue (https://motion.dev/docs/vue)及其滚动动画接口
    (https://motion.dev/docs/vue-scroll-animations)，其 Vue 声明式集成比 GSAP 更贴合当前项目。

  - 沉浸图库依据：PhotoSwipe Getting Started (https://photoswipe.com/getting-started/)。
  - Drive 同步语义依据：rclone sync (https://rclone.org/commands/rclone_sync/)。
  - 默认精选为所有相册中拍摄时间最新的照片；相册内按拍摄时间升序排列，无拍摄时间的照片排在有日期照片之
    后并按文件名排列。

  - 照片墙公开可访问，不增加后台相册编辑器、RAW/ARW 显影、视频、评论、点赞、下载原图或多用户权限。
