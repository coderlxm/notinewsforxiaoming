# Journal 验收功能清单

## 1. 交付状态

- 正式域名：`https://feeds.xmcloud.buzz`
- 正式版本：`63479b3759d6b1f79e6e7692c4694112e4560480`
- GitHub Actions 发布：Journal job 成功后，bot job 成功
- 运行分工：`bwgdc01` 是唯一 Telegram long polling 节点；`rndc02` 只运行 Journal API、网站、SQLite 和媒体存储
- 数据位置：`rndc02:/opt/journal/data/`
- 备份位置：`notinews-drive:NotiNewsBackups-LongTerm/rndc02-journal`

## 2. 已完成的线上验收

| 项目 | 结果 | 证据类型 |
| --- | --- | --- |
| HTTPS 公开站点、健康接口、公开 API | 通过 | 运行时响应 |
| HTTP 自动跳转 HTTPS | 通过 | 运行时响应 |
| `/me` 管理员密码登录 | 通过 | 运行时登录与私有列表访问 |
| 未登录不能访问私有列表 | 通过 | 运行时返回 401 |
| 内部写入接口必须提供 Bearer token | 通过 | 运行时返回 401 |
| 私有页面及鉴权接口禁止缓存 | 通过 | OpenResty 实际响应 `private, no-store` |
| RSS `/rss.xml` 与 JSON Feed `/feed.json` | 通过 | 运行时响应 |
| Journal 容器健康，端口只绑定 `127.0.0.1:3100` | 通过 | rndc02 运行时状态 |
| rndc02 原有 6 个容器保持运行 | 通过 | rndc02 运行时状态 |
| 现有 bot 保持 systemd active | 通过 | bwgdc01 运行时状态 |
| Telegram long polling 进程只有 1 个 | 通过 | bwgdc01 运行时进程状态 |
| 两个节点部署版本一致 | 通过 | 两端版本标识均为正式版本 |
| GitHub Actions 先 Journal、后 bot 的发布门 | 通过 | 首次正式发布记录 |
| Journal 首份 Google Drive 备份 | 通过 | `journal-rndc02-2026-07-19-191202.tar.gz` |
| 每日 04:50 自动备份定时器 | 通过 | rndc02 systemd timer active |
| rclone 使用既有 `notinews-drive` remote | 通过 | rndc02 rclone 1.74.4 与 remote 配置 |

## 3. 已完成的实现核对

- [x] `/note` 私有记录、`/post` 公开记录。
- [x] 命令正文、回复消息、一次性等待三条采集路径。
- [x] 一次性等待可用 `/cancel` 取消，并复用原提示消息。
- [x] 文本、图片、视频、圆形视频、动画、语音、音频、文件、贴纸及 Telegram 结构化消息映射。
- [x] 照片/视频相册聚合为一条网页记录，整组成功后才结束捕获会话。
- [x] Telegram 云端附件下载限制为单文件 20 MB，超限直接报告失败。
- [x] 原消息 `chat_id + message_id` 幂等，人工重提不会重复建立同一记录。
- [x] 公开/私有逐条切换；私有记录不进入公开详情、媒体、RSS 或 JSON Feed。
- [x] 公开单列信息流、永久详情页、标签入口和游标加载。
- [x] 私有资产搜索、标签、类型、可见性、日期筛选。
- [x] 正文编辑、置顶、往年今日。
- [x] `/me` 与 Telegram 保存结果均提供二次确认永久删除，相册按整组删除记录和附件。
- [x] `/me` 提供 Web 富文本文章创建与编辑入口，文章固定先保存为私有 Entry。
- [x] 文章支持标题、标签、段落、二三级标题、列表、引用、代码、链接、封面和文中图片。
- [x] 文中图片支持工具栏上传、粘贴、拖拽和再次插入已上传图片，并可编辑替代文字。
- [x] 文章支持私有预览、公开详情、信息流摘要、置顶、可见性切换和二次确认永久删除。
- [x] 富文本正文以 Tiptap JSON 保存，纯文本摘要由服务端派生，搜索同时匹配标题与摘要。
- [x] 公开文章进入 RSS 与 JSON Feed，正文 HTML 由固定 schema 生成并按允许列表清理。
- [x] 私有文章及图片继续使用现有管理员 Cookie；图片只能引用当前文章自己的 inline asset。
- [x] SQLite 与媒体使用同一 `/opt/journal/data/` 持久化边界。
- [x] rndc02 不包含 `bot.launch()`、webhook 或 updates 消费路径。
- [x] Journal 故障只让 Journal 操作明确失败，不替换 Telegram 通道或改变原 bot 业务。

## 4. 需要所有者首次使用确认

以下项目必须由 Telegram 账号本人发送真实消息，交付过程没有代替所有者创建测试日记，因此保留为首用确认项：

- [ ] 发送一条 `/note` 文本，在 `/me` 中看到私有记录。
- [ ] 发送一条 `/post` 文本，在公开首页看到记录，并用结果按钮转为私有。
- [ ] 回复一条既有消息发送 `/note`，确认保存的是被回复消息。
- [ ] 单独发送 `/note`，再发送一条语音或文件，确认提示消息被编辑为保存结果。
- [ ] 发送带 `/note` caption 的照片/视频相册，确认网页只出现一条记录且媒体完整。
- [ ] 在 `/me` 中完成一次搜索、正文编辑、置顶和可见性切换。
- [ ] 新建一条带附件的记录，分别确认取消删除不会改变记录、确认删除后网站和附件均不可访问。
- [ ] 在 `/me` 点击“写文章”，创建一篇带标题、标签和多种正文格式的私有文章。
- [ ] 首次保存后上传封面，并通过工具栏上传、粘贴或拖拽一张文中图片，填写替代文字后保存。
- [ ] 退出再进入编辑页，确认标题、标签、正文格式、封面和文中图片保持一致。
- [ ] 先预览私有文章，再设为公开，确认首页、永久详情、RSS 与 JSON Feed 均出现该文章。
- [ ] 从正文移除文中图片并保存，确认对应图片资产被永久删除。
- [ ] 删除一篇带封面和文中图片的文章，确认文章、详情和全部图片均不可再访问。

这些项目的最短操作说明见《简易使用手册》。
