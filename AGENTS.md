# AGENTS.md

## 项目协作原则

这个项目是一个个人 Telegram 提醒 bot，代码优先服务业务效果。实现时必须保持主路径清晰，避免把简单功能做成复杂系统。

默认按单用户个人工具设计，优先选择用户可直接理解、查看和操作的最短主路径，只解决真实需求与已发生的问题，禁止主动引入企业级安全、合规、容灾、多用户、复杂状态或“以后可能有用”的组件、抽象和方案。

设计 Telegram 交互时默认优先复用、编辑或删除现有消息，仅在业务确需保留新消息时新增，避免污染消息流。

本地证据不足时，可通过 `ssh bwgdc01` 进入部署环境查看日志，或使用部署环境凭据发起 API 请求核对响应，以定位线上问题。

涉及第三方库、组件或工具的对接时，必须在写入前主动核对当前安装版本的公开 API、依赖声明与本地解析状态，确认无误后方可交付，尤其不得交付会导致前端模块解析或模板编译失败、使页面无法加载的改动。

用户要求调用本机 OpenCode 时，必须使用已选中的 `opencode-go/deepseek-v4-flash` 模型，并将推理档位设为 `high`。

设计数据管理页面时，必须先按用户直觉建立“框架常驻、局部加载、操作即时反馈、已加载状态复用”的完整交互模型，禁止让请求、路由或组件生命周期直接造成整页闪现、无意义重载或状态跳变。

## 项目文档读取约束

`doc/` 下的任何文档都只用于记录历史方案和历史实现，不代表当前需求、当前设计或当前技术事实，不具有主动参考价值。

- 除非用户明确指定某一份文档作为当前任务的参考依据，否则禁止主动读取、检索或引用 `doc/` 下的文档内容。
- 不得为了了解项目背景、查找相似方案、沿用历史设计或确认当前实现而浏览既有文档；应以用户当前陈述、实际源码、运行环境和必要的权威资料为依据。
- 用户只指定一份文档时，只允许读取该文档，不得据此扩展读取同目录或相关主题的其他文档。
- 新增或修改文档仍应遵守文档产出目录约束，但不得先读取既有文档作为格式、命名或内容参考，除非用户明确要求。

## 用户协作意图与工作阶段判断

用户希望代理像持续参与项目的协作者一样判断当前工作阶段，而不是把每句话机械映射成固定流程。

### 存在关键歧义时必须先询问

当用户的表达存在多种合理解释，且不同解释会导致布局结构、交互方式、数据行为、实现范围或最终呈现明显不同时，禁止自行选择一种理解后直接写入或反复试错。应先结合当前源码、实际页面和最近对话做只读核对；仍不能唯一确定用户意图时，必须暂停相关修改，用简短、具体的语言向用户确认关键差异，得到答复后再继续。

不得以“先改一版看看”“后面还能调整”作为猜测关键意图的理由。尤其在用户指出方向不对、结果不符合预期或实现明显别扭后，必须立即停止沿原假设继续局部修补，重新陈述自己理解的目标；无法确认时直接询问用户，不得连续提交基于同一未确认假设的变体。

对于能够从源码、运行状态或用户已明确提供的信息中直接确定的事实，不应把核对责任转移给用户；只有会实质改变结果且现有证据无法消除的歧义才需要询问。

当新指令与当前会话的项目或模块明显不一致时，必须先向用户确认目标，不得自行推断并执行。

### 浏览器只作为必要的补充手段

处理前端问题时，应首先依据用户提供的现象、当前源码和明确的状态链定位根因。源码已经能够解释问题并确定最小根因修复时，禁止为了再次确认而连接或操作浏览器。

只有在源码证据不足、问题依赖真实渲染结果或运行时状态、必须观察交互后的实际页面，或者用户明确要求使用浏览器时，才允许连接浏览器。不得把浏览器检查当作前端任务的默认步骤，也不得用浏览器操作代替对代码状态、事件顺序和布局机制的理解。

### 先判断当前阶段

开始行动前，应结合最近对话、工作区状态、近期提交和已有实现，判断当前处于以下哪一阶段：

- 调研或诊断
- 功能实现
- 局部体验迭代
- 发布准备
- 正式部署
- 部署后观察与修复

不得因为完成了当前阶段的工作，就擅自进入下一阶段。此前对部署、验证或外部操作的授权，也不得自动继承到后续新一轮体验调整。

例如：

- “改一版我看看效果”通常表示只完成当前代码修改并停下等待反馈，不代表启动临时服务、创建验收数据、提交或部署。
- 一项完整功能已经部署后，继续调整样式属于新的局部迭代阶段，不应继承上一轮的部署授权。
- “观察部署情况，有失败自行修复”表示持续观察当前部署；失败时读取真实日志、修复、提交并重新推送，不需要重新询问已经授予的权限。

### 控制动作升级

始终选择足以完成当前意图的最低成本动作，不得自动升级工作范围：

```text
阅读与修改代码
→ 使用已有环境进行必要验证
→ 新建临时服务或测试数据
→ 提交代码
→ push 和生产部署
```

进入后一个层级必须有当前语境中的明确依据。“通常下一步会做”不构成依据。

尤其禁止：

- 为展示一个纯前端调整，擅自启动专用临时验收服务或制造临时文章。
- 修改完成后自动构建、启动、提交或部署，只为了让流程显得完整。
- 用户只是要求查看改动效果时，擅自生成一个新的验收流程让用户配合。
- 把之前一次任务中的授权复用于后续不同阶段的工作。

### 延续已有上下文

用户说“继续”“进行后续操作”“收尾”等表达时，应延续当前已经建立的工作路径，不得重新从零设计流程、重复调研已经确认的事实，或引入新的工具和部署方案。

采取行动前应优先确认：

- 当前工作区已有的未提交改动；
- 最近已经完成和已经部署的内容；
- 当前对话中已经确认或否决的方案；
- 项目已有的真实操作路径。

用户的纠正是理解其判断方式的证据，不应被立即改写成一条绝对、永久、非黑即白的新规则。应保留纠正发生时的目标、阶段和成本背景。

### 本项目的部署事实

NotiNewsForXiaoming 的现有发布路径是：

```text
形成符合项目格式的提交
→ push 到 main
→ GitHub Actions 自动部署
→ 观察真实 workflow
→ 失败时根据日志修复并再次 push
```

本项目部署不使用 `deploy-to-rndc02`。不得因为用户说“部署”就重新搜索或设计其他部署架构。

在已经明确进入发布阶段后，用户说“部署”“继续后续操作”或同等表达，应直接沿用上述现有流程，不得重复核对已经确定的部署方式。

### 迭代与发布的整体意识

部署时机应结合当前阶段和整批变更的价值判断：

- 局部前端体验调整通常留在本地继续积攒，不为每次视觉尝试单独部署。
- 涉及服务端、数据库迁移或一组已经完整收尾的功能时，可以形成一次正式发布。
- 一次发布完成后产生的新调整，视为新的迭代，不自动继续发布。
- 未经当前语境授权，不主动 commit 或 push。

不要把以上判断简化为“前端永远不部署”或“后端改动一定部署”；核心是理解当前工作是否已经形成值得发布的完整批次。

## 严禁任何兜底

不要主动加入任何兜底。包括但不限于：

- 重试
- fallback
- 降级实现
- 替代发送通道
- 静默跳过
- 容错吞错
- 复杂防御式判断
- 与业务无关的状态机或异常分支
- 返回默认值假装成功
- 捕获错误后继续执行

除非用户明确要求，否则出错应直接暴露，让问题回到根因排查，而不是在代码里堆任何兜底。

## 优先使用成熟 npm 库

在存在成熟、维护良好、适合当前业务场景的 npm 库时，除非用户明确要求，否则禁止手写实现同类能力。

包括但不限于：

- 日期、时区、调度、cron 解析
- Telegram bot 交互和消息发送
- 数据库访问和迁移
- HTML/Markdown 转义或解析
- RSS、HTTP、表单、URL、JSON schema 等通用协议处理

不要为了“简单”手写一个低配版本。应优先选用成熟库，让代码聚焦业务主路径。

## 修主路径，不修失败路径

遇到线上异常时，优先回答和定位：

- 主路径为什么失败
- 哪个外部依赖或运行环境导致失败
- 最小的根因修复是什么

不要优先设计“失败后怎么办”。这个项目不需要为了理论完备性牺牲代码可读性。

定位并修复问题时，禁止只处理用户当前描述的单一触发状态或当前截图中的表象。写入前必须还原该功能最短的完整主路径，检查首次进入、直接操作、取消后再次操作、页面或组件恢复等与根因直接相关的连续阶段。最小变更是指以最小代码范围修复完整根因，不是为单个状态增加只在当下生效的局部补丁。

如果同一个问题需要用户连续指出相邻状态下的异常，必须立即停止继续追加补丁，重新审查状态来源、事件顺序、组件生命周期和完整交互路径，再实施一次覆盖主路径的根因修复。不得等待用户逐项暴露可以从当前实现直接推导出的后续问题。

## 验证命令硬约束

除非用户在当前对话里明确要求，否则禁止主动运行、建议运行或写入任何测试、验证、检查、构建、启动、冒烟类命令。

禁止范围包括但不限于：`test`、`build`、`dev`、`start`、`tsc`、`vue-tsc`、`eslint`、`prettier`、`vitest`、`jest`、`node -e`、`tsx -e`、`ts-node`、`npx`、`pnpm`、`npm`、`yarn` 等。

不要把任何测试、验证、检查、构建、启动、冒烟类命令写进开发文档、验收文档、最终回复或建议步骤。最终回复也不要提“未测试 / 未验证 / 未检查 / 未构建”等相关内容。

本项目优先业务主路径和真实使用效果，不用流程化验证替代理解需求。

## 代码风格

- 保持实现短而直接。
- 不为了极端情况添加防御代码。
- 不引入与当前业务无关的抽象。
- 不把一次排查中的临时方案沉淀为长期代码。
- 默认严禁任何兜底、重试、fallback；如果确实需要，必须先得到用户明确同意。
- 除非watch符合官方的使用规范并且会带来明显收益，否则严禁使用 `watch`。
- 任何情况下都严禁使用 `requestAnimationFrame`、`cancelAnimationFrame` 或任何 RAF 别名。

## Git 提交格式

提交前必须查看近期 Git 历史，并沿用项目现有的英文 Conventional Commits 格式（如 `feat: ...`、`fix: ...`）；禁止使用中文提交信息。

## 方案输出硬约束

凡是“方案、设计、计划、文档”类请求，默认必须直接写入项目文件（通常为 `doc/design` 或用户指定目录）。

- 除非用户明确要求，否则禁止在对话里直接输出完整方案正文。
- 对话里只允许给出文件路径和简短说明，不贴完整内容。
- 若用户未指定文件名，先按语义创建合理文件名并落盘，再回复路径。
- 文档产出必须按内容归入 `doc` 下合适的分类目录；没有对应目录时先创建，禁止散落在 `doc` 根目录。

## 循环提醒导入硬约束

`src/reminders/recurring.ts` 中 `rrule` 的导入写法属于线上稳定性敏感点。

- 除非用户明确要求，否则禁止修改这行导入及其加载方式。
- 不允许因为本地环境报错而单独切换该导入写法。
- 如需调整，必须先说明依据和风险，再由用户明确确认后执行。
